import 'package:flutter/material.dart';

class GetHistoryPage extends StatelessWidget {
  const GetHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('History'),
        ),
        body: const Text("GetHistoryPage"));
  }
}
